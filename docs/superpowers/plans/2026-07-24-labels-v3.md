# Étiquettes course v3 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Pas de push/deploy — validation visuelle d'Adrien d'abord.

**Goal:** Étiquettes libres dans TOUT l'espace écran mais ne recouvrant jamais UI / points de passage / tracé ; étiquette Départ-Arrivée inversée (START/FINISH) ; pointillés toujours visibles (blend inversion) ; apparition garantie AVANT le passage de la tête.

## Global Constraints
- Repo C:/Dev/monolith-terrain, branche feat/orbital-globe. COMMITS LOCAUX UNIQUEMENT.
- Les étiquettes ne recouvrent JAMAIS : l'UI (profil, barre), une ancre de point de passage, le tracé GPX (échantillonné écran). Tout le reste de l'écran est permis (l'emprise du bloc n'est PLUS une limite).

### T1 — Obstacles v2 (race-labels.js + main.js)
- Bornes = viewport (suppr. clamp emprise, garder marges 4px). Supprimer getExtent du placement (garder la dep pour rien casser ou la retirer proprement).
- Nouvelle dep `getTrackWorlds()` (main.js) : ~200 Vector3 échantillonnés de track.world (recalculés au setDirty).
- Dans update() : projeter ces points → `obs` (x,y écran). `hitsObs(x,y,w,h)` = un point du tracé OU une ancre visible dans le rect gonflé de 6px, OU intersection avec rects UI (profil/barre, déplacés ici depuis la passe 4b).
- Placement : left/right piles inchangées PUIS chaque étiquette s'écarte vers SON extérieur (fx ± pas de 24px) tant que hitsObs ; top/bottom : intégrer hitsObs au while existant (pas vertical). Bornes viewport.
- Supprimer l'ancienne passe 4b (UI) devenue partie de hitsObs.

### T2 — Étiquette Départ/Arrivée inversée (race-labels.js + css + main.js)
- getItems : détection boucle (dist points[0]↔[last] < 200 m). Boucle → 1 item kind 'start', mode 'START / FINISH'. Sinon → 2 items : 'start' (START, world[0]) + 'finish' (FINISH, world[last], même gabarit). Toujours visibles (faded:false), jamais fenêtrés.
- buildNode start/finish : fond ENCRE (inversé), texte blanc (pas de texte en couleur), km dans un GROS encadré accent `<span class="rl-start-km">220 KM</span>`, `<b class="rl-start-word">START</b>` (ou FINISH / START / FINISH) en gros, pictos (≤8) EN DESSOUS du mot. Logo : `filter: brightness(0) invert(1)` (blanc sur fond sombre).
- hh du start ≈ 56 (avec rangée pictos 68).

### T3 — Pointillés toujours visibles
- `.rl-leader { mix-blend-mode: difference; border-top-color: rgba(255,255,255,.85) }` (+ anchor idem ? non — l'ancre garde l'accent).

### T4 — Apparition AVANT la tête, toujours
- Ré-estimer la vitesse tête (lissée) dans getItems ; lead = max(2 km, v*4 s) → même à vitesse folle, le fondu 1,8 s est fini avant le passage. Tail = 10 km inchangé.

### T5 — Vérif + commit local
- Transju : cibles 0 chevauchement, aucun rect sur trackPts/ancres, raz-du-sol OK (étiquettes s'étalent hors emprise), START/FINISH selon boucle. npm test + build. Commit local, PAS de push.
